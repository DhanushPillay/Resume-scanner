# Required for GitHub API verification (get from github.com/settings/tokens)
GITHUB_TOKEN=your_github_personal_access_token_here

# OPTIONAL - OpenCorporates paid API (FREE alternatives are built-in!)
# The app now uses FREE sources: UK Companies House, SEC EDGAR, DuckDuckGo, Google
# Only add this if you have a paid OpenCorporates subscription
OPENCORPORATES_API_TOKEN=
